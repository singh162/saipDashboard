export default {
	deleteImage (typePicker) {
		console.log("typePicker",typePicker);
		// // if(typePicker && typePicker.files.length>0){
		// // typePicker.files.pop();
		// // typePicker.selectedFiles.pop();
		// // }
		// resetWidget(FilePicker1,true);


	}
}